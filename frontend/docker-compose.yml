version: '3.8'
services:
  frontend:
    build: .
    ports:
      - "5173:80"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    working_dir: /usr/share/nginx/html
    networks:
      - hackathon-net
networks:
  hackathon-net:
    driver: bridge
